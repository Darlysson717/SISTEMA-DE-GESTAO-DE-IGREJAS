{
  "buildCommand": "flutter build web --release --base-href /",
  "outputDirectory": "build/web",
  "devCommand": "flutter run -d web --web-port=3000",
  "installCommand": "flutter pub get",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
